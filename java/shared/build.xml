<?xml version="1.0"?>
<project default="shared" basedir="../..">

    <!-- User settings -->
    <property file="${user.home}/ant.properties"/>
    <!-- Set property lib dir -->
    <property name="properties.dir" value="tools/ant/properties"/>
    <!-- Significant dirs -->
    <property file="${properties.dir}/dirs.properties"/>
    <!-- Compiler settings -->
    <property file="${properties.dir}/modern.properties"/>
    <!-- Compile-time classpath properties files -->

    <target name="shared">

        <echo level="info" message="building generated sanity info"/>
        <javac
                source="1.7"
                target="1.7"
                bootclasspath="${empty}"
                nowarn="on"
                debug="${debug}"
                depend="${depend}"
                deprecation="${deprecation}"
                optimize="${optimize}"
                proceed="${proceed}"
                verbose="${verbose}"
                srcdir="${generated.src.dir}/shared"
                destdir="${out.dir}">
            <classpath>
                <pathelement path="${main.classpath}"/>
            </classpath>
        </javac>

        <javac
                source="1.7"
                target="1.7"
                bootclasspath="${empty}"
                nowarn="on"
                debug="${debug}"
                depend="${depend}"
                deprecation="${deprecation}"
                optimize="${optimize}"
                proceed="${proceed}"
                verbose="${verbose}"
                srcdir="${derby.shared.src.dir}"
                includes="${splice.package}/shared/**"
                destdir="${out.dir}">
            <classpath>
                <pathelement path="${main.classpath}"/>
            </classpath>
        </javac>


    </target>

</project>

