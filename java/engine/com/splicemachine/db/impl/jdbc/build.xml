<?xml version="1.0"?>
<project default="compile" basedir="../../../../../../..">

    <!-- User settings -->
    <property file="${user.home}/ant.properties"/>
    <!-- Set property lib dir -->
    <property name="properties.dir" value="tools/ant/properties"/>
    <!-- Significant dirs -->
    <property file="${properties.dir}/dirs.properties"/>
    <!-- Compiler settings -->
    <property file="${properties.dir}/modern.properties"/>
    <!-- Compile-time classpath properties files -->
    <property file="${properties.dir}/extrapath.properties"/>

    <!-- Targets -->

    <target name="compile">

        <copy file="metadata.properties" tofile="${out.dir}/com/splicemachine/db/impl/jdbc/metadata.properties"/>

        <javac
                source="1.7"
                target="1.7"
                bootclasspath="${empty}"
                nowarn="on"
                debug="${debug}"
                depend="${depend}"
                deprecation="${deprecation}"
                optimize="${optimize}"
                proceed="${proceed}"
                verbose="${verbose}"
                srcdir="${derby.engine.src.dir}"
                destdir="${out.dir}">
            <classpath>
                <pathelement path="${log4j};${joda-time};${main.classpath}"/>
            </classpath>
            <include name="${splice.package}/impl/jdbc/*.java"/>
            <include name="${splice.package}/impl/jdbc/authentication/**"/>
            <include name="${splice.package}/impl/jdbc/*20.java"/>
            <include name="${splice.package}/impl/jdbc/*30.java"/>
            <include name="${splice.package}/impl/jdbc/LOBStoredProcedure.java"/>
            <include name="${splice.package}/impl/jdbc/authentication/JNDI**"/>
            <include name="${splice.package}/impl/jdbc/authentication/LDAP**"/>
        </javac>

    </target>

</project>

