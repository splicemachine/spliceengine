#!/bin/bash

# Cloudera VM Splice Machine starter

. ./lib/setEnv

echo "Starting Splice Machine on $SPLICE_ENV"

sudo $HBASE_HOME/bin/hbase-daemon.sh --config conf start master
sudo $HBASE_HOME/bin/hbase-daemon.sh --config conf start regionserver
