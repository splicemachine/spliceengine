#!/bin/bash

# Cloudera VM Splice Machine installer

echo "Stopping Splice Machine into Cloudera"

sudo $HBASE_HOME/bin/hbase-daemon.sh stop regionserver
sudo $HBASE_HOME/bin/hbase-daemon.sh stop master
