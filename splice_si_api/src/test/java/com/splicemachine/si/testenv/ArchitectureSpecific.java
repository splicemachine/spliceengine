package com.splicemachine.si.testenv;

/**
 * Marker interface for a test which is Architecture-specific (and therefore cannot be run in the base
 * module, but SHOULD be run in the equivalent architecture module)
 *
 * @author Scott Fines
 *         Date: 2/8/16
 */
public interface ArchitectureSpecific{ }
