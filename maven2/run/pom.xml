<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.splicemachine</groupId>
        <artifactId>db-project</artifactId>
        <version>1.1.0.1-SNAPSHOT</version>
    </parent>
    <artifactId>dbrun</artifactId>
    <name>Apache Derby Network Server Java Command</name>
    <description>Contains the Apache Derby network server, which allows remote clients to connect to Derby databases
        over a network connection using the Derby client JDBC driver.
    </description>

    <dependencies>
        <dependency>
            <groupId>com.splicemachine</groupId>
            <artifactId>db</artifactId>
            <version>1.1.0.1-SNAPSHOT</version>
            <optional>false</optional>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <configuration>
                            <tasks>

                                <copy file="${basedir}/../../jars/${sanity}/derbyrun.jar"
                                      tofile="${basedir}/target/${project.build.finalName}.jar"
                                      overwrite="true"/>
                            </tasks>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
