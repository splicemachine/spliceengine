<?xml version="1.0" encoding="UTF-8"?>
<project xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.splicemachine</groupId>
        <artifactId>db-project</artifactId>
        <version>1.1.1.6-SNAPSHOT</version>
    </parent>
    <artifactId>dbTesting</artifactId>
    <name>Apache Derby Testing</name>
    <description>Contains the Apache Derby network server, which allows remote clients to connect to Derby databases
        over a network connection using the Derby client JDBC driver.
    </description>

    <dependencies>
        <dependency>
            <groupId>com.splicemachine</groupId>
            <artifactId>db</artifactId>
            <version>1.1.1.2-SNAPSHOT</version>
            <optional>false</optional>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <artifactId>maven-antrun-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <configuration>
                            <tasks>

                                <copy file="${basedir}/../../jars/${sanity}/derbyTesting.jar"
                                      tofile="${basedir}/target/${project.build.finalName}.jar"
                                      overwrite="true"/>
                                <jar destfile="${basedir}/target/${project.build.finalName}-sources.jar">
                                    <fileset dir="${basedir}/../../java/testing"/>
                                </jar>
                            </tasks>
                        </configuration>
                        <goals>
                            <goal>run</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
