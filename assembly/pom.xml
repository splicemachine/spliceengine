<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.splicemachine</groupId>
    <artifactId>splice_machine-assembly-${envClassifier}</artifactId>
    <packaging>jar</packaging>
    <name>${project.artifactId}</name>
    <description>Assembly module for SpliceMachine</description>
    <parent>
        <artifactId>spliceengine-parent</artifactId>
        <groupId>com.splicemachine</groupId>
        <version>2.0.1.13-SNAPSHOT</version>
    </parent>
    <dependencies>
        <dependency>
            <groupId>${project.groupId}</groupId>
            <artifactId>hbase_sql-${envClassifier}</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>
    <properties>
        <jenkins.build.number>1</jenkins.build.number>
        <parcel.patch>p0.${jenkins.build.number}</parcel.patch>
    </properties>
    <profiles>
        <profile>
            <id>parcel</id>
            <activation>
                <property>
                    <name>parcel</name>
                    <value>parcel</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <!-- This used to be confinded to the spark-prepare profile, but is now done always by default
                             To disable, use skipSparkPrepare on the command line -->
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-dependency-plugin</artifactId>
                        <version>2.10</version>
                        <executions>
                            <execution>
                                <id>copy</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>copy-dependencies</goal>
                                </goals>
                                <configuration>
                                    <includeArtifactIds>
                                        hppc,kryo,gson,protobuf-java,disruptor,
                                        kryo-serializers,ehcache-core,opencsv,super-csv,
                                        lucene-core,spark,concurrentlinkedhashmap-lru,
                                        db-engine,db-drda,db-client,db-tools-ij,
                                        utilities,stats,pipeline_api,splice_access_api,splice_encoding,
                                        splice_machine,splice_protocol,splice_si_api,
                                        splice_timestamp_api,hbase_pipeline-${envClassifier},
                                        hbase_storage-${envClassifier},hbase_sql-${envClassifier},
                                        ${spark-assembly-id}
                                    </includeArtifactIds>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <executions>
                            <execution>
                                <phase>pre-integration-test</phase>
                                <configuration>
                                    <target>
                                        <replace token="TOP_VERSION" value="${project.version}.${envClassifier}.${parcel.patch}" dir="target/classes/meta">
                                            <include name="parcel.json"/>
                                        </replace>
                                        <replace token="BASE_VERSION" value="${project.version}" dir="target/classes/meta">
                                            <include name="parcel.json"/>
                                        </replace>
                                        <replace token="COMPONENT_VERSION" value="${project.version}.${envClassifier}" dir="target/classes/meta">
                                            <include name="parcel.json"/>
                                        </replace>
                                        <replace token="PKG_VERSION" value="${project.version}+${envClassifier}+${buildNumber}" dir="target/classes/meta">
                                            <include name="parcel.json"/>
                                        </replace>
                                        <replace token="PKG_RELEASE" value="1.${envClassifier}.${parcel.patch}" dir="target/classes/meta">
                                            <include name="parcel.json"/>
                                        </replace>
                                        <replace token="CDH_VERSION" value="${cdh.parcel.version}" dir="target/classes/meta">
                                            <include name="parcel.json"/>
                                        </replace>
                                        <copy todir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}/lib/splice/">
                                            <fileset dir="target/dependency" includes="**/*"/>
                                        </copy>
                                        <copy todir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}/meta">
                                            <fileset dir="target/classes/meta" includes="**/*"/>
                                        </copy>
                                        <copy todir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}/bin">
                                            <fileset dir="target/classes/bin" includes="**/*"/>
                                        </copy>
                                        <chmod dir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}/bin" perm="755" includes="**/*.sh"/>
                                        <!-- el6 -->
                                        <replace token="FULL_VERSION" value="${project.version}.${envClassifier}.${parcel.patch}-el6" dir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}/meta">
                                            <include name="parcel.json"/>
                                        </replace>
                                        <tar destfile="target/parcels/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}-el6.parcel">
                                            <tarfileset username="root" group="root" dir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}" prefix="SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}"/>
                                        </tar>
                                        <!-- el7 -->
                                        <replace token="FULL_VERSION" value="${project.version}.${envClassifier}.${parcel.patch}-el7" dir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}/meta">
                                            <include name="parcel.json"/>
                                        </replace>
                                        <tar destfile="target/parcels/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}-el7.parcel">
                                            <tarfileset username="root" group="root" dir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}" prefix="SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}"/>
                                        </tar>
                                        <!-- sles11 -->
                                        <replace token="FULL_VERSION" value="${project.version}.${envClassifier}.${parcel.patch}-sles11" dir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}/meta">
                                            <include name="parcel.json"/>
                                        </replace>
                                        <tar destfile="target/parcels/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}-sles11.parcel">
                                            <tarfileset username="root" group="root" dir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}" prefix="SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}"/>
                                        </tar>
                                        <!-- precise -->
                                        <replace token="FULL_VERSION" value="${project.version}.${envClassifier}.${parcel.patch}-precise" dir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}/meta">
                                            <include name="parcel.json"/>
                                        </replace>
                                        <tar destfile="target/parcels/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}-precise.parcel">
                                            <tarfileset username="root" group="root" dir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}" prefix="SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}"/>
                                        </tar>
                                        <!-- wheezy -->
                                        <replace token="FULL_VERSION" value="${project.version}.${envClassifier}.${parcel.patch}-wheezy" dir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}/meta">
                                            <include name="parcel.json"/>
                                        </replace>
                                        <tar destfile="target/parcels/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}-wheezy.parcel">
                                            <tarfileset username="root" group="root" dir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}" prefix="SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}"/>
                                        </tar>
                                        <!-- trusty -->
                                        <replace token="FULL_VERSION" value="${project.version}.${envClassifier}.${parcel.patch}-trusty" dir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}/meta">
                                            <include name="parcel.json"/>
                                        </replace>
                                        <tar destfile="target/parcels/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}-trusty.parcel">
                                            <tarfileset username="root" group="root" dir="target/SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}" prefix="SPLICEMACHINE-${project.version}.${envClassifier}.${parcel.patch}"/>
                                        </tar>
                                        <exec executable="python2.7" failonerror="true" dir="target/parcels">
                                            <arg line="${project.basedir}/src/main/resources/python/make_manifest.py"/>
                                        </exec>
                                    </target>
                                </configuration>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <profile>
            <id>complete</id>
            <activation>
                <property>
                    <name>complete</name>
                    <value>complete</value>
                </property>
            </activation>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-shade-plugin</artifactId>
                        <configuration>
                            <shadedArtifactAttached>true</shadedArtifactAttached>
                            <shadedClassifierName>${envClassifier}_complete</shadedClassifierName>
                            <artifactSet>
                                <includes>
                                    <include>com.carrotsearch:hppc</include>
                                    <include>com.esotericsoftware.kryo:kryo</include>
                                    <include>com.google.code.gson:gson</include>
                                    <include>com.google.protobuf:protobuf-java</include>
                                    <include>com.lmax:disruptor</include>
                                    <include>de.javakaffee:kryo-serializers</include>
                                    <include>net.sf.ehcache:ehcache-core</include>
                                    <include>net.sf.opencsv:opencsv</include>
                                    <include>net.sf.supercsv:super-csv</include>
                                    <include>org.apache.lucene:lucene-core</include>
                                    <include>org.apache.spark</include>
                                    <include>com.googlecode.concurrentlinkedhashmap:concurrentlinkedhashmap-lru</include>
                                    <include>com.splicemachine:*</include>
                                    <include>org.sparkproject.guava:*</include>
                                </includes>
                            </artifactSet>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <!-- exclude signing keys from uber-jar -->
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>**/yarn-site.xml</exclude>
                                        <exclude>**/core-site.xml</exclude>
                                        <exclude>**/splice-site.xml</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>shade</goal>
                                </goals>
                                <configuration>
                                    <transformers>
                                        <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                            <manifestEntries>
                                                <Implementation-Version>${buildNumber}</Implementation-Version>
                                                <Release>${project.version}</Release>
                                                <Build-Time>${maven.build.timestamp}</Build-Time>
                                                <URL>http://www.splicemachine.com</URL>
                                            </manifestEntries>
                                        </transformer>
                                    </transformers>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>
